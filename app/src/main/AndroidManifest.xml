<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.evolutyzitservices.projects.bloodbank2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".Home1Activity"
            android:launchMode="singleInstance"/>
        <activity android:name=".ChatMessage.ChatActivity" />
        <activity android:name=".EventActivities.EventsActivity" />
        <activity
            android:name=".DonarDetails.DonarDetailsActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".LinksActivity"/>
    </application>

</manifest>